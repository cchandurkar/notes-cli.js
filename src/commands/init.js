
var fs = require('fs');
var readline = require('readline');

module.exports = (function(){
  return {
    command: 'init',
    flags: '-i',
    alias: ['initialize'],
    execute: function(argvs){

    }
  }
}());
